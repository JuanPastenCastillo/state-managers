import styled from "styled-components"

export const INDEX_JotaiWrapper = styled.div`
  box-shadow: 1px 1px 10px white;

  border-radius: 16px;

  padding: 24px;

  & > fieldset {
    border-radius: inherit;

    display: grid;
    gap: 24px;
  }
`
